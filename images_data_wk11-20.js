// ============================================================
// COMIC PANEL IMAGES (base64) — WEEKS 11–20
// FILE CHUNKING: split across images_data_wk{N}-{N+9}.js files (10 weeks/file),
// all merging into the same global window.COMIC_IMAGES. Load in ascending order
// in index.html, before the matching data_wk*.js files.
// Each week's images use a wk{N}- key prefix, e.g. wk11-01-panel-name
// ============================================================

window.COMIC_IMAGES = window.COMIC_IMAGES || {};
Object.assign(window.COMIC_IMAGES, {
  // Week 11 images go here
});
